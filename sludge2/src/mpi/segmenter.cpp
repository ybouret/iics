#include "../segmenter.hpp"

void Segmenter:: dispatch_vertical_junctions( const mpi &MPI, const PBC &pbc )
{
    for( unit_t i=X.lower;i<=X.upper;++i)
    {
        Segment &seg = Vert(i);
        
    }
    
    
}

